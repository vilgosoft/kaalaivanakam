/**
 * Typecheck placeholder — the real UI is served from public/assets/*.js (recovered build).
 * Rebuild that bundle with deployment/package-hostinger-upload.ps1 when API/contract changes.
 */
export {}
